metadata:
  name: kubernetes-create-or-update-privileged-pod
  version: 5.0.0
  description: The following analytic detects the creation or update of privileged
    pods in Kubernetes. It identifies this activity by monitoring Kubernetes Audit
    logs for pod configurations that include root privileges. This behavior is significant
    for a SOC as it could indicate an attempt to escalate privileges, exploit the
    kernel, and gain full access to the host's namespace and devices. If confirmed
    malicious, this activity could lead to unauthorized access to sensitive information,
    data breaches, and servi
  author: Patrick Bareiss, Splunk
  tags:
  - anomaly
  - splunk-security-content
  - kubernetes-security
  mitre_attack:
    techniques:
    - id: T1204
      name: Technique T1204
    tactics: []
files:
  bundled: []
  references:
  - uri: https://media.githubusercontent.com/media/splunk/attack_data/master/datasets/attack_techniques/T1204/kubernetes_privileged_pod/kubernetes_privileged_pod.json
    description: kubernetes - _json
    format: auto
    schema: raw
    sourcetype: _json
defaults:
  replay:
    delta_factor: 1.0
    base_time: earliest
    target_time: now-1h
  schema: raw
